package com.example.giantbombapigroupproject

import org.junit.Test
import android.text.InputType
import android.widget.EditText
import com.example.giantbombapigroupproject.presenter.LoginScreenActivity
import com.nhaarman.mockito_kotlin.mock
import org.junit.Before
import org.mockito.Mockito

//TODO: Make this work Properly
class TestUnamePwordFormat {

       @Test
       fun testUnameIsEmailType(){
           //var mockUname = Mockito.mock(EditText::class.java)
           //assert(mockUname.inputType == InputType.TYPE_TEXT_VARIATION_EMAIL_ADDRESS)
       }

       @Test
       fun testPasswordIsHidden(){
           //var mockPassword = Mockito.mock(EditText::class.java)
           //mockPassword = mockLoginActivity.findViewById(R.id.password)
           //assert(mockPassword.inputType == InputType.TYPE_TEXT_VARIATION_PASSWORD)
       }
}
